package ratelimiter

// rate limiter for limiting requests to the reverse proxy or each of the webservers or both ? ( which will the better option needs to be studied)

// use token bucket algorithm and leaky bucket algorithm and compare the difference with different usecases
